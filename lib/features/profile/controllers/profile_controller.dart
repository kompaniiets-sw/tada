import 'package:get/get.dart';

class ProfileController extends GetxController {
  static ProfileController get instance => Get.find();

  void logout() {
    // Логіка виходу з профілю
  }

  void updateProfile() {
    // Логіка оновлення профілю
  }
}
